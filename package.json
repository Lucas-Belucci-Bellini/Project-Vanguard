{
  "name": "project-vanguard",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Vanguard Field — navegação de expedição com GPS, bússola, trilha local e protocolo de posição para socorro.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test \"test/**/*.test.js\"",
    "mobile:sync": "npm run build && cap sync",
    "mobile:sync:android": "npm run build && cap sync android",
    "mobile:sync:ios": "npm run build && cap sync ios",
    "mobile:android:debug": "npm run mobile:sync:android && cd android && ./gradlew assembleDebug --no-daemon",
    "mobile:ios:open": "npm run mobile:sync:ios && cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "vite": "^5.4.10"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/geolocation": "^8.2.2",
    "@capacitor/ios": "^8.5.0",
    "maplibre-gl": "^4.7.1"
  }
}
