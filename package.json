{
  "name": "project-vanguard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Project Vanguard — GPS topográfico tático + computador de tiro (estética Arma 3). Ecossistema Projeto Baluarte.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "node --test \"test/**/*.test.js\""
  },
  "devDependencies": {
    "vite": "^5.4.10"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1"
  }
}
